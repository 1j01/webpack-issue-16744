{
  "scripts": {
    "build-library": "cd library && webpack --mode=production",
    "build-app": "cd app && webpack --mode=development",
    "build": "npm run build-library && npm run build-app",
    "test": "node app/build/app-bundle.js"
  },
  "devDependencies": {
    "webpack": "5.75.0",
    "webpack-cli": "5.0.1"
  }
}
